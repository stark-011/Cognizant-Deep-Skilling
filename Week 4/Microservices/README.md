# Microservices Exercises

This folder contains Week 4 microservices practice examples:

- Student service
- Course service
- API gateway
- Shared configuration
- Health check endpoint
- Logging example
- Dockerfile and Docker Compose setup

Run each FastAPI service with Uvicorn from its own folder.

Example:

```bash
uvicorn app:app --reload --port 8001
```
