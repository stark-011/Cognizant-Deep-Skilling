# Flask Exercises

This folder contains Week 4 Flask practice examples for:

- Flask project structure
- Routing
- HTTP methods
- Path and query parameters
- Jinja2 templates
- Blueprints
- Flask extensions
- SQLAlchemy integration

Install dependencies with:

```bash
pip install -r requirements.txt
```

Run an example with:

```bash
python app.py
```
